# 网盘直链解析站（网页版）— 项目大纲 v0.1

> 状态：待 Tzz 审查
> 定位：LinkSwift 的"免安装网页版"替代品。服务担心脚本安全、嫌步骤繁琐、希望直观体验或只是偶尔使用的玩家。

---

## 0. 一句话定位

**把"装油猴脚本"变成"打开一个网页"**：输入分享链接 → 看到目录树 → 勾选文件 → 导出 aria2/gopeed 直链任务。全部数据留在用户浏览器，零上传，代码开源。

## 1. 核心原则（写进 README 的承诺）

- **零数据上传**：API 请求从用户浏览器直发网盘服务器，不经过任何中间服务器；本站不存储任何 cookie
- **开源**：代码全公开，可审计，GitHub Pages 可直接体验
- **免安装**：不需要脚本猫/篡改猴，不需要装扩展，打开即用
- **尊重隐私**：用户的历史（cookie、解析记录）只存在于用户自己的浏览器（localStorage），无续杯无遗忘之忧
- **硬核功能参考 LinkSwift**（致谢油小猴/hmjz100），本项目专注体验与安全

## 2. 目标用户与场景

| 用户类型 | 现状痛点 | 本项目的解法 |
|---|---|---|
| 普通用户 | 怕脚本不安全、嫌装脚本管理器麻烦 | 网页打开即用，代码开源可审计 |
| 偶尔使用者 | 记不住脚本怎么装 | 收藏夹一个链接，随用随开 |
| 进阶用户 | 想要目录树/批量/保留结构 | 遍历深度、复选框、目录结构保留开关 |

## 3. 功能清单（v1 范围）

### 3.1 核心流程
1. 输入/粘贴分享链接 → 自动识别网盘类型
2. 解析分享 → 获取 stoken + 文件列表
3. **目录树展示**（文件夹递归，深度可配置，默认 2 层）
4. 文件勾选（支持"全选本目录""按大小/类型过滤"）
5. 批量获取直链 → 展示结果
6. 导出：**aria2 命令 / gopeed 任务 / 复制直链列表**（保留文件夹结构开关）
7. 浏览器直接下载（可选，小文件场景）

### 3.2 游客限制处理（UC 23018 等）
- 检测到"游客可获取大小限制"错误 → **弹窗提醒**：
  - 说明：该文件超过游客限制
  - 询问：是否通过浏览器读取对应官网存储的 cookie（用户已登录时）
  - **读取一次发一次请求，本站不存储 cookie**
  - 拒绝则跳过该文件，继续处理其他文件

### 3.3 直链生命周期
- 直链有效期 3-6 小时（实测）
- 过期直链自动重新获取，配合 `-C -` 断点续传
- 大文件推荐 aria2/gopeed（多线程），内置下载任务生成器

## 4. 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 构建 | Vite + React 19 + TypeScript | 现代、快、类型安全 |
| UI | Ant Design 5 | Tree/Checkbox/Modal/Progress 现成，中文生态好 |
| 状态 | Zustand | 轻量，适合前端状态 |
| 请求 | 原生 fetch + 统一封装 | 零依赖，便于审计 |
| 样式 | antd + CSS Modules | 少而可控 |
| 部署 | GitHub Pages（静态，无后端） | 免费、可体验、代码即站点 |

## 5. 总体架构

```
┌─────────────────────────────────────────────┐
│  浏览器（用户本地）                            │
│  ┌─────────────────────────────────────┐    │
│  │  React SPA（GitHub Pages 托管）       │    │
│  │  ├─ 网盘适配器层 (PanAdapter)        │    │
│  │  │    UC 适配器（v1）                │    │
│  │  │    └─ token / detail / download   │    │
│  │  ├─ 目录树遍历引擎（递归/深度限制）    │    │
│  │  ├─ 任务生成器（aria2/gopeed/直链）   │    │
│  │  └─ Cookie 管理（localStorage）      │    │
│  └─────────────────────────────────────┘    │
│        │ fetch（浏览器直连，带 cookie）      │
└────────┼────────────────────────────────────┘
         ▼
   pc-api.uc.cn（网盘官方 API，零中转）
```

**关键点：无后端、无中间服务器。浏览器 = 执行环境 + cookie 持有者。**

## 6. 关键技术决策点（待审查 ⚠️）

### 6.1 Cookie 来源 — 最大决策点
浏览器同源策略：GitHub Pages 域名（`*.github.io`）**读不到** `drive.uc.cn` 的 cookie。
方案对比：

| 方案 | 体验 | 技术 | 风险 |
|---|---|---|---|
| **A. 书签注入（推荐）** | 用户把"解析助手"拖到书签栏一次；之后：打开分享页 → 点书签 → 自动解析 | 书签 JS 在 `drive.uc.cn` 域执行（同源），读 `document.cookie` + 调 API，UI 浮层注入页面 | 书签拖拽对小白仍有一点点门槛；部分网盘页面结构变化需适配 |
| B. 手动粘贴 cookie | 每次解析前粘贴 cookie | 网页提供输入框，用户从浏览器 DevTools 复制 | 太麻烦，体验差，但零安装 |
| C. 混合（推荐补充） | 书签为主，手动粘贴兜底 | A+B 都做 | 工作量略增 |

**倾向：A 为主 + B 兜底。** 但注意：书签注入本质是"在网盘域执行代码"，接近油猴但**免脚本管理器**——这是"网页"与"脚本"的折中，需要你确认是否接受。

> 备选：如果 CORS 实测允许（从 github.io fetch pc-api.uc.cn 不拦），则纯网页方案可行，cookie 只能手动粘贴。**上线前需做 CORS 冒烟测试。**

### 6.2 CORS 验证（上线前必做）
- 从 `*.github.io` 页面 fetch `pc-api.uc.cn` 是否被 `Access-Control-Allow-Origin` 拦截？
- 若拦截：书签注入方案天然规避（同源执行）；纯网页方案死路
- 若放行：纯网页 + 手动 cookie 可行

### 6.3 多网盘扩展性
- v1 只做 UC（当前已验证全链路）
- 适配器接口预留：`PanAdapter { detect(url), getToken(), getTree(), getLinks() }`
- 后续网盘按接口插拔

### 6.4 已知技术细节（从逆向笔记带入）
- UC API host: `pc-api.uc.cn`
- 关键参数：`file/download` 必须带 `?entry=ft&fr=pc&pr=UCBrowser`（漏了必 401）
- 必须带完整 cookie 组（含 `__pugs` 指纹 cookie）+ UC 客户端 UA
- stoken 从 `sharepage/token` 获取；目录遍历用 `sharepage/detail?pdir_fid=`
- 错误码：`31001` 需登录 / `23018` 游客超限
- 直链是 OSS 签名 URL，字符不可改动（一个字符错 → SignatureDoesNotMatch）
- 直链 3-6h 有效，可断点续传

## 7. 代码规范

```
src/
├── adapters/          # 网盘适配器（uc.ts 为 v1 实现）
│   └── types.ts       # PanAdapter 接口定义
├── core/
│   ├── treeWalker.ts  # 目录树递归遍历（深度限制/并发控制）
│   ├── cookieStore.ts # localStorage 封装 + 过期策略
│   └── errors.ts      # 错误码映射（23018/31001 → 友好文案）
├── components/        # 目录树、勾选、进度、弹窗
├── pages/             # 主页面（输入/结果）
├── tasks/             # aria2/gopeed 命令生成器
└── utils/
```

- TypeScript `strict` 模式
- ESLint + Prettier 统一格式
- 适配器接口先行，UC 实现遵循接口
- 错误全部走统一 ErrorBoundary + 错误码映射表
- 不引任何埋点/统计库（隐私承诺）
- 提交信息中文，README 中英双语

## 8. GitHub Pages 体验版

- `vite build` 产出纯静态，`base: './'` 相对路径
- 部署到 `gh-pages` 分支 / GitHub Actions 自动构建
- README 顶部放体验链接 + 使用教程（GIF 动图演示书签安装）
- 页面底部：开源声明 + 零上传承诺 + 致谢 LinkSwift

## 9. 风险与对策

| 风险 | 对策 |
|---|---|
| 网盘改版/接口变动 | 适配器隔离；错误提示引导用户等待更新；社区反馈渠道 |
| 书签被浏览器策略限制 | 提供手动粘贴兜底；文档说明 |
| 游客限制加剧 | 弹窗引导登录读取 cookie（不存储） |
| GitHub Pages 被墙 | 提供 Docker 自托管 / 本地 `npm run dev` 说明 |
| 大文件下载断流 | 直链续命 + 断点续传 + 推荐 aria2/gopeed |

## 10. 里程碑

- M0：CORS 冒烟测试 + 书签注入可行性验证（半天）
- M1：UC 适配器 + 目录树遍历引擎（核心，1-2 天）
- M2：React UI（输入→树→勾选→导出）（1-2 天）
- M3：Cookie 方案落地（书签/粘贴/持久化）（1 天）
- M4：GitHub Pages 部署 + README + 演示（半天）
- M5：真实链接全流程回归测试（UC 大文件 + 游客限制场景）

## 11. 待 Tzz 确认的问题

1. **执行形态**：接受"书签注入为主"吗？还是坚持"纯网页打开即用"（需接受手动粘贴 cookie）？
2. **UI 风格**：antd 默认风格即可，还是想要更"极客/清新"的定制主题？
3. **浏览器直连下载**要不要进 v1？（大文件 aria2 为主，小文件可浏览器下）
4. **命名**：项目叫什么？暂定 `pan-parse-web` / `云链解析站`（可改）
5. **支持网盘范围**：v1 只做 UC，还是先把适配器骨架搭好预留 2-3 个网盘位？
